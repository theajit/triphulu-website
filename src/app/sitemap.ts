import type { MetadataRoute } from 'next'; import { features } from '@/content'; export const dynamic='force-static'; export default function sitemap():MetadataRoute.Sitemap{const base='https://www.triphulu.com';const routes=['','/features','/pricing','/about','/contact','/security','/privacy','/terms',...features.map(f=>`/features/${f.slug}`)];return routes.map(route=>({url:`${base}${route}`,lastModified:new Date(),changeFrequency:route===''?'weekly':'monthly',priority:route===''?1:route==='/features'?.9:.7}))}
