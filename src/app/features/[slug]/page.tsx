import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import Link from '@/components/Link'; import { features } from '@/content'; import { CTA } from '@/components/CTA'; import { ProductMockup } from '@/components/ProductMockup';
export function generateStaticParams(){return features.map(f=>({slug:f.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const f=features.find(x=>x.slug===slug);return f?{title:f.title,description:f.outcome}:{} }
export default async function FeaturePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const f=features.find(x=>x.slug===slug);if(!f)notFound();return <><section className="page-hero feature-hero"><div className="shell two-col"><div><p className="eyebrow">{f.eyebrow} / TripHulu</p><h1>{f.title}</h1><p className="lead">{f.summary}</p><div className="actions"><Link className="button" href="/contact">Book a demo <span>↗</span></Link><Link className="text-link" href="/features">All features →</Link></div></div><ProductMockup mode={slug==='operations'?'ops':'pipeline'}/></div></section><section className="section"><div className="shell detail-grid"><div><p className="eyebrow">The business outcome</p><h2>{f.outcome}</h2>{f.note&&<p className="note">{f.note}</p>}</div><div className="point-list">{f.points.map((p,i)=><div key={p}><span>{String(i+1).padStart(2,'0')}</span><h3>{p}</h3></div>)}</div></div></section>{f.flow&&<section className="flow-section"><div className="shell"><div className="section-heading"><p className="eyebrow">How it moves</p><h2>A clear path from start to next action.</h2></div><div className="flow compact">{f.flow.map((x,i)=><div key={x}><span>{String(i+1).padStart(2,'0')}</span><b>{x}</b></div>)}</div></div></section>}<CTA/></>}
