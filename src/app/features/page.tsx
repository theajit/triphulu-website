import type { Metadata } from 'next'; import Link from '@/components/Link'; import { features } from '@/content'; import { CTA } from '@/components/CTA';
export const metadata:Metadata={title:'Features',description:'Explore TripHulu’s connected sales, booking, payment and tour operations workflows.'};
const groups=['Sell','Book','Operate','Plan','Communicate','Manage','Assure'];
export default function Features(){return <><section className="page-hero"><div className="shell"><p className="eyebrow">Product overview</p><h1>One trip record.<br/><em>Every team in sync.</em></h1><p className="lead">TripHulu joins the commercial and operational stages of a journey, so work moves forward with its context intact.</p></div></section><section className="section"><div className="shell catalog">{groups.map(g=><div className="catalog-group" key={g}><div><span>{g}</span></div><div>{features.filter(f=>f.eyebrow===g).map(f=><Link href={`/features/${f.slug}`} key={f.slug}><h2>{f.title}</h2><p>{f.summary}</p><b>View feature →</b></Link>)}</div></div>)}</div></section><CTA/></>}
