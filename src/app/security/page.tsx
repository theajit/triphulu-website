import type { Metadata } from 'next'; import { CTA } from '@/components/CTA';
export const metadata:Metadata={title:'Security & platform practices',description:'An overview of TripHulu’s tenant isolation, role access and release assurance practices.'};
const items=[['Tenant isolation','Travel businesses operate within tenant-specific environments, with data boundaries designed around each organization.'],['Role-based access','Sales, Operations and administrative responsibilities can be separated through role-aware access.'],['Release assurance','Automated regression can be combined with manual quality checks before important releases.'],['Private evidence','Quality artifacts and screenshots are treated as private operating material, not public marketing content.'],['Payment configuration','Provider connections and verification workflows depend on the configuration approved for each tenant.'],['Responsible disclosure','Contact our team to report a suspected security issue so it can be reviewed through the appropriate channel.']];
export default function Security(){return <><section className="page-hero"><div className="shell"><p className="eyebrow">Security & platform practices</p><h1>Trust is part of<br/><em>the operating workflow.</em></h1><p className="lead">TripHulu’s platform practices are designed to keep tenant context, user responsibilities and release evidence appropriately separated.</p></div></section><section className="section"><div className="shell policy-grid">{items.map((x,i)=><article key={x[0]}><span>0{i+1}</span><h2>{x[0]}</h2><p>{x[1]}</p></article>)}</div><div className="shell disclaimer">This page is a high-level overview, not a certification statement or contractual security schedule. Formal commitments should be confirmed with TripHulu.</div></section><CTA/></>}
